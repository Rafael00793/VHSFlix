rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite leitura e escrita global para garantir sincronização instantânea e robusta entre todos os dispositivos
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

